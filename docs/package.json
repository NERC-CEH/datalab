{
  "name": "docs",
  "private": true,
  "devDependencies": {
    "markdownlint-cli": "^0.26.0",
    "shx": "^0.3.2"
  },
  "scripts": {
    "lint": "markdownlint ./src",
    "update-version": "shx echo \"version: `git describe --tags --always`\" > version.md"
  }
}
