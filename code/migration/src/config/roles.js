export const PROJECT = 'project';

export const INSTANCE_ADMIN = 'instance-admin';
export const ADMIN = `${PROJECT}:admin`;
export const USER = `${PROJECT}:user`;
export const VIEWER = `${PROJECT}:viewer`;
