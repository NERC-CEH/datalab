---
- name: Setup ufw
  ufw:
    state: enabled
    policy: deny

- name: Allow ssh traffic from machines allowed to manage this node
  ufw:
    rule: allow
    port: "{{ ssh_port }}"
    from_ip: "{{ item }}"
    proto: tcp
  with_items: "{{ management_ip_addresses }}"
